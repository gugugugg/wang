AI Context: Gobang Scaling Laws Research Manifest (v3.0)🛠️ 第一部分：AI 系统提示词 (System Prompt)如果你将代码发送给其他 AI（如 Claude 或 GPT-4）进行修改，请先输入这段指令：Role: 你现在是一名 AAAI 级别的深度学习科研助手，专注于强化学习与博弈论。Objective: 协助研究五子棋 AI 的智能程度（Win Rate）如何随模型规模（Model Scale $N$）与训练数据量（Data $D$）的变化而演化。Core Architecture: SE-ResNet + Dual Head (Policy/Value)。Contract: 模型必须严格返回 (policy_logits, value) 元组；训练需支持 8 路对称增强；推理需兼容 Numba 加速的 MCTS。Tone: 保持专业且极具工程严谨性，所有修改必须考虑 8 并发下的 I/O 稳定性和硬件热管理。🧠 第二部分：逻辑宣言 (Logic Manifest)1. 论文核心论点 (Thesis Hypotheses)本项目旨在通过实证研究证明：五子棋 AI 的智能程度与参数量及训练步数遵循特定的 Scaling Laws。计算公式：智能程度 $I \approx f(Filters, Cycles)$。对比变量：规模 ($N$)：Small (16 filters), Medium (64 filters), Large (128 filters)。深度 ($D$)：1 ~ 100 轮蒸馏迭代 (Cycles)，每轮产生 500 局高质量专家数据。2. 核心架构协议 (Technical Protocol)骨干网络：SE-ResNet。利用通道注意力机制（Squeeze-and-Excitation）捕捉棋盘全局空间依赖。激活函数：全网统一使用 Swish (SiLU)，以提供比 ReLU 更平滑的梯度流，助力深层模型收敛。随机开局协议：为了对抗先手必胜坍塌，必须执行 randomize_opening=True，在天元周围随机预置棋子。3. 数据生产流水线 (Data Pipeline)专家蒸馏：调用 Yixin 引擎进行 8 并发同步对弈，获取 Ground Truth。数据增强：执行 8 路对称变换（旋转 90/180/270° 及镜像翻转），将 5 万局原始对局扩充为 40 万局有效样本。学习率策略：使用 Cosine Annealing LR，$T_{max}$ 必须与实验总轮数（100）保持严格同步。⚠️ 第三部分：易出现问题的地方总结 (Hazard Map)在进行 100 轮实验的长跑中，以下地方最容易导致崩溃或实验失败：1. 硬件与资源崩溃 (Hardware Hazards)热降频导致的超时：若 CPU 长期处于 95°C，频率下降会导致引擎在 get_move 时超过 20s 限制，进而引发子进程崩溃。显存堆积 (OOM)：如果 worker.py 在每轮结束后没有显式调用 torch.cuda.empty_cache()，显存碎片会随轮次增加，最终导致训练中断。2. 算法逻辑缺陷 (Algorithmic Pitfalls)元组解构错误：这是最常见的代码级报错。task_train.py 在计算 Loss 时必须正确接收模型返回的元组。若修改架构时改变了返回格式，训练会立即报错。哈希一致性漂移：Zobrist 哈希表在 Alpha-Beta 搜索中若发生 XOR 更新错误（如撤销步逻辑不对），会导致置换表（Transposition Table）失效，AI 表现出极低的智商。协议不匹配：如果设置了 100 轮实验但 T_max 留在了 50，则模型在后 50 轮的学习率会由于重启周期而过高，导致无法完成最终收敛。3. 数据完整性问题 (Data Integrity)僵尸进程堆积：8 并发环境下，若 task_gen.py 的异常处理不当，会导致 pbrain-Yixin2018.exe 进程在后台堆积，耗尽系统句柄，最终导致蓝屏。胜负标签丢失：SGF 解析时若未正确提取 RE[...] 标签，会导致 Value 网络学到全是 0 的垃圾数据，模型将失去全局胜负观。📂 模块映射与维护指引训练控制：worker.py (管理时间采样与蒸馏循环)。生成加速：task_gen.py (8 并发引擎调度与持久化通信)。模型定义：model.py (SE-Block 与 Swish 实现)。环境核心：game_env.py (随机开局与胜负判定逻辑)。